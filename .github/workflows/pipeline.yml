name: pipeline

on: [ push, pull_request ]

jobs:
    linting:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: actions/setup-node@v5
              with:
                node-version: '22'
            - run: npm ci
            - run: npm run lint
    
